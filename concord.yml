flows:
  default:
  - log: "Hello Concord User"
  onPush:
  - log: "${event.sender} pushed ${event.commitId} to ${event.payload.repository.full_name}"  
  flow1:
  - log: "Test flow1"
  
  
triggers:
- github:
    entryPoint: "onPush"
    conditions:
      type: "push"  

